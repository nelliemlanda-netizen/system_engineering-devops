#!/usr/bin/env bash
# Sends SIGTERM to all running 7-highlander processes

# Find all PIDs of 7-highlander
pids=$(pgrep -x 7-highlander)

# Send SIGTERM to each PID
for pid in $pids; do
    kill -SIGTERM "$pid"
done
