#!/usr/bin/env bash
# Display the PID and full path of all processes whose name contains 'bash'
ps -eo pid,comm= | grep '[b]ash' | while read -r pid cmd; do
    full_path=$(readlink -f /proc/"$pid"/exe 2>/dev/null)
    echo "$pid $full_path"
done
