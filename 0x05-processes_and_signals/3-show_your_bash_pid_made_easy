#!/usr/bin/env bash
# Display the PID and full command path of all processes whose name contains 'bash'
ps -C bash -o pid= -o comm= | while read pid cmd; do
    # Get full path of command
    full_cmd=$(readlink -f /proc/"$pid"/exe 2>/dev/null)
    echo "$pid $full_cmd"
done
